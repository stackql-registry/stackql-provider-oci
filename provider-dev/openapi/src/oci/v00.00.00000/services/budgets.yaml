openapi: 3.0.0
info:
  title: budgets API
  description: oci budgets API
  version: '20190111'
paths:
  /budgets:
    get:
      description: |
        Gets a list of budgets in a compartment.

        By default, ListBudgets returns budgets of the 'COMPARTMENT' target type, and the budget records with only one target compartment OCID.

        To list all budgets, set the targetType query parameter to ALL (for example: 'targetType=ALL').

        Clients should ignore new targetTypes, or upgrade to the latest version of the client SDK to handle new targetTypes.
      operationId: ListBudgets
      parameters:
        - $ref: '#/components/parameters/CompartmentIdQueryParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/LifecycleStateQueryParam'
        - $ref: '#/components/parameters/DisplayNameQueryParam'
        - $ref: '#/components/parameters/TargetTypeQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of BudgetSummary objects.
          headers:
            opc-next-page:
              description: |
                For pagination of a list of `Budget`s. If this header appears in the response, then this
                is a partial list of budgets. Include this value as the `page` parameter in a subsequent
                GET request to get the next batch of budgets.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListBudgetsResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a list of Budgets
      tags:
        - budget
    post:
      description: |
        Creates a new budget.
      operationId: CreateBudget
      parameters:
        - $ref: '#/components/parameters/RetryTokenHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBudgetDetails'
        description: Details for the new budget.
        required: true
      responses:
        '200':
          description: The budget was successfully created.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Creates a new Budget
      tags:
        - budget
      x-example: |
        POST /20190111/budgets
        Host: usage.us-phoenix-1.oci.oraclecloud.com
        <var>&lt;authorization and other headers&gt;</var>
        {
          "compartmentId": "ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq",
          "targetType" : "COMPARTMENT",
          "targets" : ["ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq"],
          "amount": "100.00",
          "resetPeriod": "Monthly"
        }
  /budgets/{budgetId}:
    delete:
      description: Deletes a specified budget resource.
      operationId: DeleteBudget
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '204':
          description: The budget was successfully deleted.
          headers:
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Delete a Budget
      tags:
        - budget
      x-related-resource: '#/definitions/Budget'
    get:
      description: Gets a budget by the identifier.
      operationId: GetBudget
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: Retrieves the budget with the given OCID.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a Budget
      tags:
        - budget
    put:
      description: Update a budget identified by the OCID.
      operationId: UpdateBudget
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBudgetDetails'
        description: The information to be updated.
        required: true
      responses:
        '200':
          description: The budget was successfully updated.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Updates a Budget
      tags:
        - budget
  /budgets/{budgetId}/alertRules:
    get:
      description: |
        Returns a list of Alert Rules for a specified budget.
      operationId: ListAlertRules
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/LifecycleStateQueryParam'
        - $ref: '#/components/parameters/DisplayNameQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of AlertRuleSummary objects.
          headers:
            opc-next-page:
              description: |
                For pagination of a list of `AlertRuleSummary`. If this header appears in the response, then this
                is a partial list of AlertRuleSummaries. Include this value as the `page` parameter in a subsequent
                GET request to get the next batch of AlertRuleSummaries.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListAlertRulesResponse'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Gets a list of all Alert Rules for a Budget
      tags:
        - budget
    post:
      description: |
        Creates a new Alert Rule.
      operationId: CreateAlertRule
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/RetryTokenHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAlertRuleDetails'
        description: Details for the new Alert Rule.
        required: true
      responses:
        '200':
          description: The Alert Rule was successfully created.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRule'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Creates a new Alert Rule
      tags:
        - budget
  /budgets/{budgetId}/alertRules/{alertRuleId}:
    delete:
      description: Deletes a specified Alert Rule resource.
      operationId: DeleteAlertRule
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/AlertRuleIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '204':
          description: The Alert Rule was successfully deleted.
          headers:
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Delete an Alert Rule
      tags:
        - budget
      x-related-resource: '#/definitions/AlertRule'
    get:
      description: Gets an Alert Rule for a specified budget.
      operationId: GetAlertRule
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/AlertRuleIdPathParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: Retrieves the Alert Rule with the given OCID.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRule'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Get an Alert Rule for a specified budget
      tags:
        - budget
    put:
      description: Update an Alert Rule for the budget identified by the OCID.
      operationId: UpdateAlertRule
      parameters:
        - $ref: '#/components/parameters/BudgetIdPathParam'
        - $ref: '#/components/parameters/AlertRuleIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAlertRuleDetails'
        description: The information to be updated.
        required: true
      responses:
        '200':
          description: The Alert Rule was successfully updated.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If
                you need to contact Oracle about a particular request,
                please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlertRule'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Updates an Alert Rule
      tags:
        - budget
  /costAlertSubscriptions:
    get:
      description: |
        Gets a list of Cost Alert Subscription in a compartment.
      operationId: ListCostAlertSubscriptions
      parameters:
        - $ref: '#/components/parameters/CompartmentIdQueryParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/NameQueryParam'
        - $ref: '#/components/parameters/CostAlertSubscriptionLifecycleStateQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of CostAlertSubscriptionSummary objects.
          headers:
            opc-next-page:
              description: |
                For list pagination. When this header appears in the response, additional pages
                of results remain. For important details about how pagination works, see
                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAlertSubscriptionCollection'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a list of Cost Alert Subscriptions
      tags:
        - costAd
    post:
      description: |
        Creates a new CostAlert Subscription.
      operationId: CreateCostAlertSubscription
      parameters:
        - $ref: '#/components/parameters/RetryTokenHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCostAlertSubscriptionDetails'
        description: Details for the new subscription.
        required: true
      responses:
        '200':
          description: The costAlertSubscription was successfully created.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAlertSubscription'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Creates a new Cost Alert Subscription
      tags:
        - costAd
      x-example: |
        POST /20190111/costAlertSubscription
        Host: usage.us-phoenix-1.oci.oraclecloud.com
        <var>&lt;authorization and other headers&gt;</var>
        {
          "compartmentId": "ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq",
          "name": "Department A email list",
          "channels" : {"email": ["abc123@example.com","example-user@domain-ex.com"]}
        }
  /costAlertSubscriptions/{costAlertSubscriptionId}:
    delete:
      description: Deletes a specified CostAlertSubscription resource.
      operationId: DeleteCostAlertSubscription
      parameters:
        - $ref: '#/components/parameters/CostAlertSubscriptionIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '204':
          description: CostAlertSubscription is successfully deleted.
          headers:
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Delete a CostAlertSubscription
      tags:
        - costAd
      x-related-resource: '#/definitions/CostAlertSubscription'
    get:
      description: Gets a CostAlertSubscription by the identifier.
      operationId: GetCostAlertSubscription
      parameters:
        - $ref: '#/components/parameters/CostAlertSubscriptionIdPathParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: Retrieves the CostAlertSubscription with the given OCID.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAlertSubscription'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a CostAlertSubscription
      tags:
        - costAd
    put:
      description: Update a CostAlertSubscription identified by the OCID.
      operationId: UpdateCostAlertSubscription
      parameters:
        - $ref: '#/components/parameters/CostAlertSubscriptionIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCostAlertSubscriptionDetails'
        description: The information to be updated.
        required: true
      responses:
        '200':
          description: The CostAlertSubscription was successfully updated.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAlertSubscription'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Updates a CostAlertSubscription
      tags:
        - costAd
  /costAnomalyEventAnalytics:
    get:
      description: |
        Gets a list of Cost Anomaly Events analytics summary - aggregated metrics for a given time period.
      operationId: SummarizeCostAnomalyEventAnalytics
      parameters:
        - $ref: '#/components/parameters/CompartmentIdQueryParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/NameQueryParam'
        - $ref: '#/components/parameters/CostMonitorIdQueryParam'
        - $ref: '#/components/parameters/TargetTenantIdQueryParam'
        - $ref: '#/components/parameters/StartDateQueryParam'
        - $ref: '#/components/parameters/EndDateQueryParam'
        - $ref: '#/components/parameters/RegionQueryParam'
        - $ref: '#/components/parameters/CostImpactQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of CostAnomalyEventAnalyticSummary objects.
          headers:
            opc-next-page:
              description: |
                For list pagination. When this header appears in the response, additional pages
                of results remain. For important details about how pagination works, see
                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyEventAnalyticCollection'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a list of Cost Anomaly Events analytics summary.
      tags:
        - costAd
      x-related-resource: '#/definitions/CostAnomalyEvent'
  /costAnomalyEvents:
    get:
      description: |
        Gets a list of Cost Anomaly Event in a compartment.
      operationId: ListCostAnomalyEvents
      parameters:
        - $ref: '#/components/parameters/CompartmentIdQueryParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/NameQueryParam'
        - $ref: '#/components/parameters/CostMonitorIdQueryParam'
        - $ref: '#/components/parameters/TargetTenantIdQueryParam'
        - $ref: '#/components/parameters/StartDateQueryParam'
        - $ref: '#/components/parameters/EndDateQueryParam'
        - $ref: '#/components/parameters/RegionQueryParam'
        - $ref: '#/components/parameters/CostImpactQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of CostAnomalyEventSummary objects.
          headers:
            opc-next-page:
              description: |
                For list pagination. When this header appears in the response, additional pages
                of results remain. For important details about how pagination works, see
                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyEventCollection'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a list of Cost Anomaly Event
      tags:
        - costAd
  /costAnomalyEvents/{costAnomalyEventId}:
    get:
      description: Gets a CostAnomalyEvent by the identifier.
      operationId: GetCostAnomalyEvent
      parameters:
        - $ref: '#/components/parameters/CostAnomalyEventIdPathParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: Retrieves the CostAnomalyEvent with the given OCID.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyEvent'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a CostAnomalyEvent
      tags:
        - costAd
    put:
      description: Update a CostAnomalyEvent identified by the OCID.
      operationId: UpdateCostAnomalyEvent
      parameters:
        - $ref: '#/components/parameters/CostAnomalyEventIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCostAnomalyEventDetails'
        description: The information to be updated.
        required: true
      responses:
        '200':
          description: The CostAnomalyEvent was successfully updated.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyEvent'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Updates a CostAnomalyEvent
      tags:
        - costAd
  /costAnomalyMonitors:
    get:
      description: |
        Gets a list of Cost Anomaly Monitors in a compartment.
      operationId: ListCostAnomalyMonitors
      parameters:
        - $ref: '#/components/parameters/CompartmentIdQueryParam'
        - $ref: '#/components/parameters/PaginationLimitQueryParam'
        - $ref: '#/components/parameters/PaginationTokenQueryParam'
        - $ref: '#/components/parameters/SortOrderQueryParam'
        - $ref: '#/components/parameters/SortByQueryParam'
        - $ref: '#/components/parameters/CostAnomalyMonitorLifecycleStateQueryParam'
        - $ref: '#/components/parameters/NameQueryParam'
        - $ref: '#/components/parameters/TargetTenantIdQueryParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: A page of CostAnomalyMonitorSummary objects.
          headers:
            opc-next-page:
              description: |
                For list pagination. When this header appears in the response, additional pages
                of results remain. For important details about how pagination works, see
                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitorCollection'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a list of Cost Anomaly Monitors
      tags:
        - costAd
    post:
      description: |
        Creates a new costAnomaly Monitor.
      operationId: CreateCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/RetryTokenHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCostAnomalyMonitorDetails'
        description: Details for the new monitor.
        required: true
      responses:
        '200':
          description: The cost anomaly monitor is successfully created.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitor'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Creates a new costAnomaly Monitor
      tags:
        - costAd
      x-example: |
        POST /20250520/costAnomalyMonitor
        Host: usage.us-phoenix-1.oci.oraclecloud.com
        <var>&lt;authorization and other headers&gt;</var>
        {
          compartmentId: "ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq",
          name: "sample cost monitor",
          targetResourceFilter: {
            operator: "AND",
            filters: [
              {
                operator: "OR",
                dimensions: [
                  {key: "compartment",value: "ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrtge6tg"},
                  {key: "compartment",value: "ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrtbefww"}
                ],
                tags: [],
                filters: []
              },
              {
                operator: "OR",
                dimensions: [
                  {key: "service",value: "compute"},
                  {key: "service",value: "network"}
                ],
                tags: [],
                filters: []
              },
              {
                operator: "OR",
                dimensions: [],
                tags: [
                  {namespace: "Oracle-Tags", key: "CreatedBy", value: "ocid1.user.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrfffebde"},
                  {namespace: "Oracle-Tags", key: "CreatedBy", value: "ocid1.user.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrpounnhr"}
                ],
                filters: []
              }
            ]
            dimensions: [],
            tags: []
          }
        }
  /costAnomalyMonitors/{costAnomalyMonitorId}:
    delete:
      description: Deletes a specified CostAnomalyMonitor resource.
      operationId: DeleteCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/CostAnomalyMonitorIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '204':
          description: The CostAnomalyMonitor is successfully deleted.
          headers:
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Delete a CostAnomalyMonitor
      tags:
        - costAd
      x-related-resource: '#/definitions/CostAnomalyMonitor'
    get:
      description: Gets a CostAnomalyMonitor by the identifier.
      operationId: GetCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/CostAnomalyMonitorIdPathParam'
        - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        '200':
          description: Retrieves the CostAnomalyMonitor with the given OCID.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitor'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Returns a CostAnomalyMonitor
      tags:
        - costAd
    put:
      description: Update a CostAnomalyMonitor identified by the OCID.
      operationId: UpdateCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/CostAnomalyMonitorIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCostAnomalyMonitorDetails'
        description: The information to be updated.
        required: true
      responses:
        '200':
          description: The Cost Anomaly Monitor is successfully updated.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitor'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Updates a Cost Anomaly Monitor
      tags:
        - costAd
  /costAnomalyMonitors/{costAnomalyMonitorId}/actions/disable:
    post:
      description: Disables the cost anomaly monitor. This stops cost anomaly detection for targeted resource(s).
      operationId: DisableCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/CostAnomalyMonitorIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
        - $ref: '#/components/parameters/RetryTokenHeader'
      responses:
        '200':
          description: The cost anomaly monitor has been disabled.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitor'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Disables the cost anomaly monitor.
      tags:
        - costAd
      x-obmcs-terraform:
        actionType: DISABLE
        identifier: CostAnomalyMonitorEnableToggle
      x-related-resource: '#/definitions/CostAnomalyMonitor'
  /costAnomalyMonitors/{costAnomalyMonitorId}/actions/enable:
    post:
      description: Enables the cost anomaly monitor. This (re)starts the cost anomaly detection for targeted resource(s).
      operationId: EnableCostAnomalyMonitor
      parameters:
        - $ref: '#/components/parameters/CostAnomalyMonitorIdPathParam'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/RequestIdHeader'
        - $ref: '#/components/parameters/RetryTokenHeader'
      responses:
        '200':
          description: The cost anomaly monitor has been enabled.
          headers:
            etag:
              description: |
                For optimistic concurrency control. See `if-match`.
              schema:
                type: string
            opc-request-id:
              description: |
                Unique Oracle-assigned identifier for the request. If you need to contact
                Oracle about a particular request, please provide the request ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CostAnomalyMonitor'
        '400':
          $ref: '#/components/responses/400'
        '401':
          $ref: '#/components/responses/401'
        '404':
          $ref: '#/components/responses/404'
        '409':
          $ref: '#/components/responses/409'
        '412':
          $ref: '#/components/responses/412'
        '429':
          $ref: '#/components/responses/429'
        '500':
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Enables the cost anomaly monitor.
      tags:
        - costAd
      x-obmcs-terraform:
        actionType: ENABLE
        identifier: CostAnomalyMonitorEnableToggle
      x-related-resource: '#/definitions/CostAnomalyMonitor'
components:
  schemas:
    BudgetSummary:
      description: A budget.
      properties:
        actualSpend:
          description: The actual spend in currency for the current budget cycle.
          minimum: 0
          type: number
        alertRuleCount:
          description: The total number of alert rules in the budget.
          minimum: 0
          type: integer
        amount:
          description: |
            The amount of the budget, expressed in the currency of the customer's rate card.
          exclusiveMinimum: true
          minimum: 0
          type: number
        budgetProcessingPeriodStartOffset:
          default: 1
          description: The number of days offset from the first day of the month, at which the budget processing period starts. In months that have fewer days than this value, processing will begin on the last day of that month. For example, for a value of 12, processing starts every month on the 12th at midnight.
          exclusiveMaximum: false
          exclusiveMinimum: false
          maximum: 31
          minimum: 1
          type: integer
        compartmentId:
          description: The OCID of the compartment.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          type: string
        displayName:
          description: The display name of the budget. Avoid entering confidential information.
          type: string
        endDate:
          description: The time when the one-time budget concludes. For example, - `2023-08-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        forecastedSpend:
          description: The forecasted spend in currency by the end of the current budget cycle.
          minimum: 0
          type: number
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the budget.
          type: string
        lifecycleState:
          description: The current state of the budget.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
          x-obmcs-top-level-enum: '#/definitions/LifecycleState'
        processingPeriodType:
          description: |
            The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.
          enum:
            - INVOICE
            - MONTH
            - SINGLE_USE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
        resetPeriod:
          description: |
            The reset period for the budget.
          enum:
            - MONTHLY
          type: string
          x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
        startDate:
          description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only string, integer and boolean types are supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing system tags' keys and values scoped to a namespace.
              Example: `{"bar-key": "value"}`
            type: object
          description: |
            Usage of system tag keys. These predefined keys are scoped to namespaces.
            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetCompartmentId:
          description: |
            This is DEPRECATED. For backwards compatability, the property is populated when
            the targetType is "COMPARTMENT", and the targets contain the specific target compartment OCID.
            For all other scenarios, this property is left empty.
          type: string
        targetType:
          description: |
            The type of target on which the budget is applied.
          enum:
            - COMPARTMENT
            - TAG
          type: string
          x-obmcs-top-level-enum: '#/definitions/TargetType'
        targets:
          description: |
            The list of targets on which the budget is applied.
              If the targetType is "COMPARTMENT", the targets contain the list of compartment OCIDs.
              If the targetType is "TAG", the targets contain the list of cost tracking tag identifiers in the form of "{tagNamespace}.{tagKey}.{tagValue}".
          items:
            type: string
          type: array
        timeCreated:
          description: The time the budget was created.
          format: date-time
          type: string
        timeSpendComputed:
          description: The time the budget spend was last computed.
          format: date-time
          type: string
        timeUpdated:
          description: The time the budget was updated.
          format: date-time
          type: string
        version:
          description: The version of the budget. Starts from 1 and increments by 1.
          type: integer
      required:
        - id
        - compartmentId
        - displayName
        - amount
        - resetPeriod
        - timeCreated
        - timeUpdated
        - alertRuleCount
        - lifecycleState
      type: object
    CreateBudgetDetails:
      description: |
        The create budget details.

        Clients should use 'targetType' and 'targets' to specify the target type and list of targets on which the budget is applied.

        For backwards compatibility, 'targetCompartmentId' is still supported for all existing clients.
        This is considered deprecated, however, and all clients are upgraded to use 'targetType' and 'targets'.

        Specifying both 'targetCompartmentId' and 'targets' causes a Bad Request.
      properties:
        amount:
          description: |
            The amount of the budget expressed as a whole number in the currency of the customer's rate card.
          exclusiveMaximum: true
          exclusiveMinimum: true
          maximum: 1000000000000
          minimum: 0
          type: number
        budgetProcessingPeriodStartOffset:
          default: 1
          description: The number of days offset from the first day of the month, at which the budget processing period starts. In months that have fewer days than this value, processing will begin on the last day of that month. For example, for a value of 12, processing starts every month on the 12th at midnight.
          exclusiveMaximum: false
          exclusiveMinimum: false
          maximum: 31
          minimum: 1
          type: integer
        compartmentId:
          description: The OCID of the compartment.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          maxLength: 200
          type: string
        displayName:
          description: The displayName of the budget. Avoid entering confidential information.
          maxLength: 200
          type: string
        endDate:
          description: The date when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        processingPeriodType:
          description: |
            The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.
          enum:
            - INVOICE
            - MONTH
            - SINGLE_USE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
        resetPeriod:
          description: |
            The reset period for the budget.
          enum:
            - MONTHLY
          type: string
          x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
        startDate:
          description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only string, integer and boolean types are supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing system tags' keys and values scoped to a namespace.
              Example: `{"bar-key": "value"}`
            type: object
          description: |
            Usage of system tag keys. These predefined keys are scoped to namespaces.
            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetCompartmentId:
          description: |
            This is DEPRECATED. Set the target compartment ID in targets instead.
          type: string
        targetType:
          default: COMPARTMENT
          description: |
            The type of target on which the budget is applied.
          enum:
            - COMPARTMENT
            - TAG
          type: string
          x-obmcs-top-level-enum: '#/definitions/TargetType'
        targets:
          description: |
            The list of targets on which the budget is applied.
              If targetType is "COMPARTMENT", the targets contain the list of compartment OCIDs.
              If targetType is "TAG", the targets contain the list of cost tracking tag identifiers in the form of "{tagNamespace}.{tagKey}.{tagValue}".
            Curerntly, the array should contain exactly one item.
          items:
            type: string
          type: array
      required:
        - compartmentId
        - amount
        - resetPeriod
      type: object
    Budget:
      description: A budget.
      properties:
        actualSpend:
          description: The actual spend in currency for the current budget cycle.
          minimum: 0
          type: number
        alertRuleCount:
          description: The total number of alert rules in the budget.
          minimum: 0
          type: integer
        amount:
          description: |
            The amount of the budget expressed in the currency of the customer's rate card.
          exclusiveMinimum: true
          minimum: 0
          type: number
        budgetProcessingPeriodStartOffset:
          default: 1
          description: The number of days offset from the first day of the month, at which the budget processing period starts. In months that have fewer days than this value, processing will begin on the last day of that month. For example, for a value of 12, processing starts every month on the 12th at midnight.
          exclusiveMaximum: false
          exclusiveMinimum: false
          maximum: 31
          minimum: 1
          type: integer
        compartmentId:
          description: The OCID of the compartment.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          type: string
        displayName:
          description: The display name of the budget. Avoid entering confidential information.
          type: string
        endDate:
          description: The time when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        forecastedSpend:
          description: The forecasted spend in currency by the end of the current budget cycle.
          minimum: 0
          type: number
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the budget.
          type: string
        lifecycleState:
          description: The current state of the budget.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
          x-obmcs-top-level-enum: '#/definitions/LifecycleState'
        processingPeriodType:
          description: |
            The budget processing period type. Valid values are INVOICE, MONTH, and SINGLE_USE.
          enum:
            - INVOICE
            - MONTH
            - SINGLE_USE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
        resetPeriod:
          description: |
            The reset period for the budget.
          enum:
            - MONTHLY
          type: string
          x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
        startDate:
          description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only string, integer and boolean types are supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing system tags' keys and values scoped to a namespace.
              Example: `{"bar-key": "value"}`
            type: object
          description: |
            Usage of system tag keys. These predefined keys are scoped to namespaces.
            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetCompartmentId:
          description: |
            This is DEPRECATED. For backwards compatability, the property is populated when
            the targetType is "COMPARTMENT", and targets contain the specific target compartment OCID.
            For all other scenarios, this property will be left empty.
          type: string
        targetType:
          description: |
            The type of target on which the budget is applied.
          enum:
            - COMPARTMENT
            - TAG
          type: string
          x-obmcs-top-level-enum: '#/definitions/TargetType'
        targets:
          description: |
            The list of targets on which the budget is applied.
              If the targetType is "COMPARTMENT", the targets contain the list of compartment OCIDs.
              If the targetType is "TAG", the targets contain the list of cost tracking tag identifiers in the form of "{tagNamespace}.{tagKey}.{tagValue}".
          items:
            type: string
          type: array
        timeCreated:
          description: The time that the budget was created.
          format: date-time
          type: string
        timeSpendComputed:
          description: The time that the budget spend was last computed.
          format: date-time
          type: string
        timeUpdated:
          description: The time that the budget was updated.
          format: date-time
          type: string
        version:
          description: The version of the budget. Starts from 1 and increments by 1.
          type: integer
      required:
        - id
        - compartmentId
        - displayName
        - amount
        - resetPeriod
        - timeCreated
        - timeUpdated
        - alertRuleCount
        - lifecycleState
      type: object
    UpdateBudgetDetails:
      description: The update budget details.
      properties:
        amount:
          description: |
            The amount of the budget expressed as a whole number in the currency of the customer's rate card.
          exclusiveMaximum: true
          exclusiveMinimum: true
          maximum: 1000000000000
          minimum: 0
          type: number
        budgetProcessingPeriodStartOffset:
          default: 1
          description: The number of days offset from the first day of the month, at which the budget processing period starts. In months that have fewer days than this value, processing will begin on the last day of that month. For example, for a value of 12, processing starts every month on the 12th at midnight.
          exclusiveMaximum: false
          exclusiveMinimum: false
          maximum: 31
          minimum: 1
          type: integer
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          maxLength: 200
          type: string
        displayName:
          description: The displayName of the budget. Avoid entering confidential information.
          maxLength: 200
          type: string
        endDate:
          description: The time when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        processingPeriodType:
          description: |
            The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.
          enum:
            - INVOICE
            - MONTH
            - SINGLE_USE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
        resetPeriod:
          description: |
            The reset period for the budget.
          enum:
            - MONTHLY
          type: string
          x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
        startDate:
          description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`. The date-time format conforms to RFC 3339, and will be truncated to the starting point of the date provided after being converted to UTC time.
          format: date-time
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only string, integer and boolean types are supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing system tags' keys and values scoped to a namespace.
              Example: `{"bar-key": "value"}`
            type: object
          description: |
            Usage of system tag keys. These predefined keys are scoped to namespaces.
            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
      type: object
    AlertRuleSummary:
      description: The alert rule.
      properties:
        budgetId:
          description: The OCID of the budget.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the alert rule.
          type: string
        displayName:
          description: The name of the alert rule. Avoid entering confidential information.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the alert rule.
          type: string
        lifecycleState:
          description: The current state of the alert rule.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
          x-obmcs-top-level-enum: '#/definitions/LifecycleState'
        message:
          description: The custom message that will be sent when the alert is triggered.
          type: string
        recipients:
          description: The audience that receives the alert when it triggers.
          type: string
        threshold:
          description: |
            The threshold for triggering the alert. If the thresholdType is PERCENTAGE, the maximum value is 10000.
          exclusiveMinimum: true
          minimum: 0
          type: number
        thresholdType:
          description: The type of threshold.
          enum:
            - PERCENTAGE
            - ABSOLUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ThresholdType'
        timeCreated:
          description: The time when the budget was created.
          format: date-time
          type: string
        timeUpdated:
          description: The time when the budget was updated.
          format: date-time
          type: string
        type:
          description: |
            ACTUAL means the alert triggers based on actual usage.
            FORECAST means the alert triggers based on predicted usage.
          enum:
            - ACTUAL
            - FORECAST
          type: string
          x-obmcs-top-level-enum: '#/definitions/AlertType'
        version:
          description: The version of the alert rule. Starts from 1 and increments by 1.
          type: integer
      required:
        - id
        - budgetId
        - displayName
        - type
        - threshold
        - thresholdType
        - recipients
        - timeCreated
        - timeUpdated
        - lifecycleState
      type: object
    CreateAlertRuleDetails:
      description: The create alert rule details. This is a batch-create.
      properties:
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the alert rule.
          maxLength: 200
          type: string
        displayName:
          description: The name of the alert rule. Avoid entering confidential information.
          maxLength: 200
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        message:
          description: The message to be sent to the recipients when the alert rule is triggered.
          maxLength: 1000
          type: string
        recipients:
          description: The audience that receives the alert when it triggers. An empty string is interpreted as null.
          type: string
        threshold:
          description: |
            The threshold for triggering the alert, expressed as a whole number or decimal value.
            If the thresholdType is ABSOLUTE, the threshold can have at most 12 digits before the decimal point, and up to two digits after the decimal point.
            If the thresholdType is PERCENTAGE, the maximum value is 10000 and can have up to two digits after the decimal point.
          exclusiveMaximum: true
          exclusiveMinimum: true
          maximum: 1000000000000
          minimum: 0
          type: number
        thresholdType:
          description: The type of threshold.
          enum:
            - PERCENTAGE
            - ABSOLUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ThresholdType'
        type:
          description: |
            The type of the alert. Valid values are ACTUAL (the alert triggers based on actual usage), or
            FORECAST (the alert triggers based on predicted usage).
          enum:
            - ACTUAL
            - FORECAST
          type: string
          x-obmcs-top-level-enum: '#/definitions/AlertType'
      required:
        - type
        - threshold
        - thresholdType
      type: object
    AlertRule:
      description: The alert rule.
      properties:
        budgetId:
          description: The OCID of the budget.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the alert rule.
          type: string
        displayName:
          description: The name of the alert rule. Avoid entering confidential information.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the alert rule.
          type: string
        lifecycleState:
          description: The current state of the alert rule.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
          x-obmcs-top-level-enum: '#/definitions/LifecycleState'
        message:
          description: Custom message sent when an alert is triggered.
          type: string
        recipients:
          description: |
            The delimited list of email addresses to receive the alert when it triggers.
            Delimiter characters can be a comma, space, TAB, or semicolon.
          type: string
        threshold:
          description: |
            The threshold for triggering the alert. If the thresholdType is PERCENTAGE, the maximum value is 10000.
          exclusiveMinimum: true
          minimum: 0
          type: number
        thresholdType:
          description: The type of threshold.
          enum:
            - PERCENTAGE
            - ABSOLUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ThresholdType'
        timeCreated:
          description: The time the budget was created.
          format: date-time
          type: string
        timeUpdated:
          description: The time the budget was updated.
          format: date-time
          type: string
        type:
          description: |
            The type of the alert. Valid values are ACTUAL (the alert triggers based on actual usage), or
            FORECAST (the alert triggers based on predicted usage).
          enum:
            - ACTUAL
            - FORECAST
          type: string
          x-obmcs-top-level-enum: '#/definitions/AlertType'
        version:
          description: The version of the alert rule. Starts from 1 and increments by 1.
          type: integer
      required:
        - id
        - budgetId
        - displayName
        - type
        - threshold
        - thresholdType
        - recipients
        - timeCreated
        - timeUpdated
        - lifecycleState
      type: object
    UpdateAlertRuleDetails:
      description: The update alert rule details.
      properties:
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the alert rule.
          maxLength: 200
          type: string
        displayName:
          description: The name of the alert rule. Avoid entering confidential information.
          maxLength: 200
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        message:
          description: The message to be delivered to the recipients when an alert is triggered.
          maxLength: 1000
          type: string
        recipients:
          description: The audience that receives the alert when it triggers. If you need to clear out this value, pass in an empty string instead of a null value.
          type: string
        threshold:
          description: |
            The threshold for triggering the alert, expressed as a whole number or decimal value.
            If the thresholdType is ABSOLUTE, the threshold can have at most 12 digits before the decimal point, and up to two digits after the decimal point.
            If the thresholdType is PERCENTAGE, the maximum value is 10000 and can have up to two digits after the decimal point.
          exclusiveMaximum: true
          exclusiveMinimum: true
          maximum: 1000000000000
          minimum: 0
          type: number
        thresholdType:
          description: The type of threshold.
          enum:
            - PERCENTAGE
            - ABSOLUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/ThresholdType'
        type:
          description: |
            The type of the alert. Valid values are ACTUAL (the alert triggers based on actual usage), or
            FORECAST (the alert triggers based on predicted usage).
          enum:
            - ACTUAL
            - FORECAST
          type: string
          x-obmcs-top-level-enum: '#/definitions/AlertType'
      type: object
    CostAlertSubscriptionCollection:
      description: Results of a costAlertSubscription search. Contains both CostAlertSubscriptionSummary items and other data.
      properties:
        items:
          description: A page of CostAnomalyMonitorSummary objects.
          items:
            $ref: '#/components/schemas/CostAlertSubscriptionSummary'
          type: array
      required:
        - items
      type: object
    CreateCostAlertSubscriptionDetails:
      description: |
        The create CostAlertSubscriptionDetails details.
        The clients should pass compartmentId where the resource should belong, description and channels.
      properties:
        channels:
          description: The notification channels string.
          maxLength: 1000
          minLength: 1
          type: string
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the cost alert subscription.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Anomaly Subscription.
          maxLength: 255
          minLength: 1
          type: string
        name:
          description: The name of the cost alert subscription. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        type:
          description: Type of alert subscription
          enum:
            - DEFAULT
            - CUSTOM
          type: string
          x-obmcs-top-level-enum: '#/definitions/MonitorType'
      required:
        - compartmentId
        - name
        - type
        - channels
      type: object
    CostAlertSubscription:
      description: A CostAlertSubscription.
      properties:
        channels:
          description: The notification channels string.
          maxLength: 1000
          minLength: 1
          type: string
        compartmentId:
          description: The OCID of the compartment which hold the cost alert subscription resource.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the cost alert subscription.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Alert Subscription.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost alert subscription.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
        name:
          description: The name of the cost alert subscription. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        timeCreated:
          description: The time that the cost alert subscription was created.
          format: date-time
          type: string
        timeUpdated:
          description: The time that the cost alert subscription was updated.
          format: date-time
          type: string
        type:
          description: Type of alert subscription
          enum:
            - DEFAULT
            - CUSTOM
          type: string
          x-obmcs-top-level-enum: '#/definitions/MonitorType'
      required:
        - id
        - compartmentId
        - name
        - lifecycleState
        - channels
        - timeCreated
        - timeUpdated
      type: object
    UpdateCostAlertSubscriptionDetails:
      description: |
        The update CostAlertSubscriptionDetails details.
      properties:
        channels:
          description: The notification channels string.
          maxLength: 1000
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the cost alert subscription.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
      type: object
    CostAnomalyEventAnalyticCollection:
      description: Results of a CostAnomalyEventAnalytics search.
      properties:
        items:
          description: The list of CostAnomalyEvent Analytic summary.
          items:
            $ref: '#/components/schemas/CostAnomalyEventAnalyticSummary'
          type: array
      required:
        - items
      type: object
    CostAnomalyEventCollection:
      description: Results of a costAnomalyEvent search. Contains both CostAnomalyEventSummary items and other data.
      properties:
        items:
          description: A page of CostAnomalyMonitorSummary objects.
          items:
            $ref: '#/components/schemas/CostAnomalyEventSummary'
          type: array
      required:
        - items
      type: object
    CostAnomalyEvent:
      description: A CostAnomalyEvent.
      properties:
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        costImpact:
          description: The cost impact of the detected anomaly.
          format: double
          type: number
        costMonitorId:
          description: The OCID of the associated cost monitor.
          maxLength: 255
          minLength: 1
          type: string
        costMonitorName:
          description: The name of the associated cost monitor.
          maxLength: 255
          minLength: 1
          type: string
        costVariancePercentage:
          description: The cost variance percentage of the detected anomaly.
          format: double
          type: number
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        feedbackResponse:
          description: The feedback response for the cost anomaly event.
          enum:
            - ACCURATE_ANOMALY
            - EXPECTED_ANOMALY
            - NOT_AN_ANOMALY
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Anomaly Event.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost anomaly event.
          enum:
            - ACTIVE
            - INACTIVE
          type: string
        rootCauseDetail:
          $ref: '#/components/schemas/RootCauseDetail'
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetResourceFilter:
          $ref: '#/components/schemas/TargetResourceFilter'
        timeAnomalyEventDate:
          description: The event date of the anomaly event.
          format: date-time
          type: string
        timeCreated:
          description: The created time of the cost anomaly event.
          format: date-time
          type: string
        timeUpdated:
          description: The updated time of the cost anomaly event.
          format: date-time
          type: string
      required:
        - id
        - compartmentId
        - costMonitorId
        - lifecycleState
        - costMonitorName
        - targetResourceFilter
        - timeAnomalyEventDate
        - timeCreated
        - timeUpdated
      type: object
    UpdateCostAnomalyEventDetails:
      description: |
        The update CostAnomalyEvent details.
      properties:
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        feedbackResponse:
          description: The feedback response for the cost anomaly event.
          type: string
          x-obmcs-enumref: '#/definitions/CostAnomalyEvent/feedbackResponse'
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
      type: object
    CostAnomalyMonitorCollection:
      description: Results of a costAnomalyMonitor search. Contains both CostAnomalyMonitorSummary items and other data.
      properties:
        items:
          description: A page of CostAnomalyMonitorSummary objects.
          items:
            $ref: '#/components/schemas/CostAnomalyMonitorSummary'
          type: array
      required:
        - items
      type: object
    CreateCostAnomalyMonitorDetails:
      description: |
        The create CostAnomalyMonitor details.
        The clients should pass ocid of the compartmentId for the monitor resource to be created. tenantIdFilter and resourceFilter
        targets resources to monitor in a given tenancy.
      properties:
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        costAlertSubscriptionMap:
          $ref: '#/components/schemas/CostAlertSubscriptionMap'
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the cost anomaly monitor.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        name:
          description: The name of the cost anomaly monitor. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        targetResourceFilter:
          $ref: '#/components/schemas/TargetResourceFilter'
      required:
        - compartmentId
        - name
        - targetResourceFilter
      type: object
    CostAnomalyMonitor:
      description: A CostAnomalyMonitor.
      properties:
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        costAlertSubscriptionMap:
          $ref: '#/components/schemas/CostAlertSubscriptionMap'
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Anomaly Monitor.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleDetails:
          description: |
            The current state details of the cost monitor.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost monitor.
          enum:
            - ACTIVE
            - INACTIVE
            - DELETED
          type: string
        name:
          description: The name of the cost anomaly monitor. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetResourceFilter:
          $ref: '#/components/schemas/TargetResourceFilter'
        timeCreated:
          description: The time that the cost monitor was created.
          format: date-time
          type: string
        timeUpdated:
          description: The time that the cost monitor was last updated.
          format: date-time
          type: string
        type:
          description: Type of cost monitor
          enum:
            - DEFAULT
            - CUSTOM
          type: string
          x-obmcs-top-level-enum: '#/definitions/MonitorType'
      required:
        - id
        - compartmentId
        - name
        - lifecycleState
        - targetResourceFilter
        - timeCreated
        - timeUpdated
      type: object
    UpdateCostAnomalyMonitorDetails:
      description: Update CostAnomalyMonitor details.
      properties:
        costAlertSubscriptionMap:
          $ref: '#/components/schemas/CostAlertSubscriptionMap'
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the cost anomaly monitor.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
      type: object
    Error:
      description: Error Information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
        - code
        - message
      type: object
    CostAlertSubscriptionSummary:
      description: A CostAlertSubscription.
      properties:
        compartmentId:
          description: The OCID of the compartment which hold the cost alert subscription resource.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Alert Subscription.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost alert subscription.
          type: string
          x-obmcs-enumref: '#/definitions/CostAlertSubscription/lifecycleState'
        name:
          description: The name of the cost alert subscription. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
      required:
        - id
        - compartmentId
        - name
        - lifecycleState
      type: object
    CostAnomalyEventAnalyticSummary:
      description: A CostAnomalyEventAnalyticSummary.
      properties:
        averageCostImpact:
          description: The average cost impact of the anomaly events in the given time period.
          format: double
          type: number
        averageCostVariance:
          description: The average cost variance of the anomaly events in the given time period.
          format: double
          type: number
        count:
          description: The number of cost anomaly events in the given time period.
          type: integer
        timeAnomalyEventEndDate:
          description: startDate for anomaly event date.
          format: date-time
          type: string
        timeAnomalyEventStartDate:
          description: startDate for anomaly event date.
          format: date-time
          type: string
      required:
        - timeAnomalyEventStartDate
        - timeAnomalyEventEndDate
        - count
        - averageCostImpact
        - averageCostVariance
      type: object
    CostAnomalyEventSummary:
      description: A CostAnomalyEvent.
      properties:
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        costImpact:
          description: The cost impact of the detected anomaly.
          format: double
          type: number
        costMonitorId:
          description: The OCID of the associated cost monitor.
          maxLength: 255
          minLength: 1
          type: string
        costMonitorName:
          description: The name of the associated cost monitor.
          maxLength: 255
          minLength: 1
          type: string
        costVariancePercentage:
          description: The cost variance percentage of the detected anomaly.
          format: double
          type: number
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Anomaly Event.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost anomaly event.
          type: string
          x-obmcs-enumref: '#/definitions/CostAnomalyEvent/lifecycleState'
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetResourceFilter:
          $ref: '#/components/schemas/TargetResourceFilter'
        timeAnomalyEventDate:
          description: The event date of the anomaly event.
          format: date-time
          type: string
      required:
        - id
        - compartmentId
        - costMonitorId
        - costMonitorName
        - targetResourceFilter
        - timeAnomalyEventDate
      type: object
    RootCauseDetail:
      description: The root cause details of the cost anomaly event.
      properties:
        value:
          description: The json object representing the root cause details of the anomaly event. (opaque JSON object)
          type: string
      type: object
    TargetResourceFilter:
      description: |
        The filter object to target resources for cost monitor. 
        Cost generated by one or more resources identified by this 
        filter is monitored for anomalous deviations.
      properties:
        dimensions:
          description: The dimensions to filter on.
          items:
            $ref: '#/components/schemas/Dimension'
          maxItems: 100
          type: array
        filters:
          description: The nested filter object.
          items:
            $ref: '#/components/schemas/TargetResourceFilter'
          maxItems: 100
          type: array
        operator:
          description: 'The filter operator. Example: ''AND'', ''OR'', ''NOT''.'
          enum:
            - AND
            - NOT
            - OR
          type: string
        tags:
          description: The tags to filter on.
          items:
            $ref: '#/components/schemas/Tag'
          maxItems: 100
          type: array
      type: object
    CostAnomalyMonitorSummary:
      description: A CostAnomalyMonitor.
      properties:
        compartmentId:
          description: The OCID of the compartment.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.

              Example: `{"CostCenter": "42"}`
            type: object
          description: |
            Defined tags for this resource. Each key is predefined and scoped to a
            namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Operations": {"CostCenter": "42"}}`
          type: object
        description:
          description: The description of the budget.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: |
            Free-form tags for this resource. Each tag is a simple key-value pair with no
            predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

            Example: `{"Department": "Finance"}`
          type: object
        id:
          description: The OCID of the Cost Anomaly Monitor.
          maxLength: 255
          minLength: 1
          type: string
        lifecycleState:
          description: The current state of the cost monitor.
          type: string
          x-obmcs-enumref: '#/definitions/CostAnomalyMonitor/lifecycleState'
        name:
          description: The name of the cost anomaly monitor. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: |-
                The value of the tag. Only the String type is supported.
                 (opaque JSON object)
              type: string
            description: |
              Key-value pair representing a defined tag key and value, scoped to a namespace.
              Example: `{"free-tier-retained": "true"}`
            type: object
          description: |
            System tags for this resource. Each key is predefined and scoped to a namespace.
            For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).
            System tags can be viewed by users, but can only be created by the system.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
          type: object
        targetResourceFilter:
          $ref: '#/components/schemas/TargetResourceFilter'
        timeCreated:
          description: The time that the cost monitor was created.
          format: date-time
          type: string
        timeUpdated:
          description: The time that the cost monitor was last updated.
          format: date-time
          type: string
        type:
          description: Type of cost monitor
          enum:
            - DEFAULT
            - CUSTOM
          type: string
          x-obmcs-top-level-enum: '#/definitions/MonitorType'
      required:
        - id
        - compartmentId
        - name
        - lifecycleState
        - targetResourceFilter
        - timeCreated
        - timeUpdated
      type: object
    CostAlertSubscriptionMap:
      description: The mapping of cost monitor to alert subscription along with thresholds.
      properties:
        costAlertSubscriptionId:
          description: The costAlertSubscription ocid which the cost monitor alert maps to.
          type: string
        operator:
          description: 'The filter operator. Example: ''AND'', ''OR'', ''NOT''.'
          enum:
            - AND
            - NOT
            - OR
          type: string
        thresholdAbsoluteValue:
          description: The absolute threshold value.
          minimum: 0
          type: integer
        thresholdRelativePercent:
          description: The relative percentage threshold value.
          minimum: 0
          type: integer
      type: object
    Dimension:
      description: |
        The dimension used for filtering. Available dimensions are: "service", "skuName", "skuPartNumber", "unit", "compartmentName", "compartmentPath", "compartmentId", "tenantName, "tenantId", "platform", "region", "logicalAd", "resourceId".
        For example:
        `[{value: "COMPUTE", key: "service"}]`
      properties:
        key:
          description: The dimension key.
          maxLength: 255
          minLength: 1
          type: string
        value:
          description: The dimension value.
          maxLength: 255
          minLength: 1
          type: string
      required:
        - key
        - value
      type: object
    Tag:
      description: The tag used for filtering.
      properties:
        key:
          description: The tag key.
          maxLength: 255
          minLength: 1
          type: string
        namespace:
          description: The tag namespace.
          maxLength: 255
          minLength: 1
          type: string
        value:
          description: The tag value.
          maxLength: 100
          minLength: 1
          type: string
      type: object
    ListBudgetsResponse:
      type: object
      properties:
        budgets:
          type: array
          items:
            $ref: '#/components/schemas/BudgetSummary'
    ListAlertRulesResponse:
      type: object
      properties:
        alert_rules:
          type: array
          items:
            $ref: '#/components/schemas/AlertRuleSummary'
  responses:
    '400':
      description: Bad Request
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '401':
      description: Unauthorized
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '404':
      description: Not Found
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '409':
      description: Conflict
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If you need to contact Oracle about a
            particular request, please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '412':
      description: Precondition failed
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '429':
      description: Too Many Requests
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    '500':
      description: Internal Server Error
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    default:
      description: Unknown Error
      headers:
        opc-request-id:
          description: |
            Unique Oracle-assigned identifier for the request. If
            you need to contact Oracle about a particular request,
            please provide the request ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  parameters:
    CompartmentIdQueryParam:
      description: The ID of the compartment in which to list resources.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
    PaginationLimitQueryParam:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        minimum: 1
        maximum: 1000
        default: 25
    PaginationTokenQueryParam:
      description: The page token representing the page at which to start retrieving results. This is usually retrieved from a previous list call.
      in: query
      name: page
      x-default-description: ''
      schema:
        type: string
        minLength: 1
    SortOrderQueryParam:
      description: The sort order to use, either 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-default-description: ''
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
          - ASC
          - DESC
    SortByQueryParam:
      description: |
        The field to sort by. If not specified, the default is timeCreated.
        The default sort order for timeCreated is DESC.
        The default sort order for displayName is ASC in alphanumeric order.
      in: query
      name: sortBy
      x-obmcs-top-level-enum: '#/definitions/SortBy'
      schema:
        type: string
        enum:
          - timeCreated
          - displayName
        default: timeCreated
    LifecycleStateQueryParam:
      description: The current state of the resource to filter by.
      in: query
      name: lifecycleState
      x-default-description: ''
      x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      schema:
        type: string
        enum:
          - ACTIVE
          - INACTIVE
    DisplayNameQueryParam:
      description: |
        A user-friendly name. This does not have to be unique, and it's changeable.

        Example: `My new resource`
      in: query
      name: displayName
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    TargetTypeQueryParam:
      description: |
        The type of target to filter by:
          * ALL - List all budgets
          * COMPARTMENT - List all budgets with targetType == "COMPARTMENT"
          * TAG - List all budgets with targetType == "TAG"
      in: query
      name: targetType
      x-default-description: ''
      schema:
        type: string
        enum:
          - ALL
          - COMPARTMENT
          - TAG
    RequestIdHeader:
      description: The client request ID for tracing.
      in: header
      name: opc-request-id
      schema:
        type: string
    RetryTokenHeader:
      description: |
        A token that uniquely identifies a request so it can be retried, in case of a timeout or
        server error, without risk of executing that same action again. Retry tokens expire after 24
        hours, but can be invalidated before then due to conflicting operations. For example, if a resource
        has been deleted and purged from the system, then a retry of the original creation request
        might be rejected.
      in: header
      name: opc-retry-token
      required: false
      schema:
        type: string
        minLength: 1
        maxLength: 64
    BudgetIdPathParam:
      description: The unique budget OCID.
      in: path
      name: budgetId
      required: true
      x-default-description: ''
      schema:
        type: string
    IfMatchHeader:
      description: |
        For optimistic concurrency control. In the PUT or DELETE call
        for a resource, set the `if-match` parameter to the value of the
        etag from a previous GET or POST response for that resource.
        The resource will be updated or deleted only if the etag you
        provide matches the resource's current etag value.
      in: header
      name: if-match
      required: false
      schema:
        type: string
    AlertRuleIdPathParam:
      description: The unique Alert Rule OCID.
      in: path
      name: alertRuleId
      required: true
      x-default-description: ''
      schema:
        type: string
    NameQueryParam:
      description: |
        Unique, non-changeable resource name.
      in: query
      name: name
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    CostAlertSubscriptionLifecycleStateQueryParam:
      description: The current state of the cost alert subscription.
      in: query
      name: lifecycleState
      x-obmcs-enumref: '#/definitions/CostAlertSubscription/lifecycleState'
      schema:
        type: string
    CostAlertSubscriptionIdPathParam:
      description: The unique costAlertSubscription OCID.
      in: path
      name: costAlertSubscriptionId
      required: true
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    CostMonitorIdQueryParam:
      description: The cost monitor ocid.
      in: query
      name: costAnomalyMonitorId
      required: true
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    TargetTenantIdQueryParam:
      description: The target tenantId ocid filter param.
      in: query
      name: targetTenantId
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    StartDateQueryParam:
      description: startDate for anomaly event date.
      in: query
      name: timeAnomalyEventStartDate
      required: true
      x-default-description: ''
      schema:
        type: string
        format: date-time
    EndDateQueryParam:
      description: endDate for anomaly event date.
      in: query
      name: timeAnomalyEventEndDate
      required: true
      x-default-description: ''
      schema:
        type: string
        format: date-time
    RegionQueryParam:
      description: region of the anomaly event.
      in: query
      name: region
      required: true
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    CostImpactQueryParam:
      description: cost impact of the anomaly event.
      in: query
      name: costImpact
      required: true
      x-default-description: ''
      schema:
        type: number
        format: double
    CostAnomalyEventIdPathParam:
      description: The unique costAnomalyEventId OCID.
      in: path
      name: costAnomalyEventId
      required: true
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
    CostAnomalyMonitorLifecycleStateQueryParam:
      description: The current state of the cost monitor.
      in: query
      name: lifecycleState
      x-obmcs-enumref: '#/definitions/CostAnomalyMonitor/lifecycleState'
      schema:
        type: string
    CostAnomalyMonitorIdPathParam:
      description: The unique costAnomalyMonitor OCID.
      in: path
      name: costAnomalyMonitorId
      required: true
      x-default-description: ''
      schema:
        type: string
        minLength: 1
        maxLength: 255
  x-stackQL-resources:
    budgets:
      id: oci.budgets.budgets
      name: budgets
      title: Budgets
      methods:
        list:
          operation:
            $ref: '#/paths/~1budgets/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.budgets
            overrideMediaType: application/json
            schema_override:
              $ref: '#/components/schemas/ListBudgetsResponse'
            transform:
              body: |-
                {{- $wrapped := printf "{\"budgets\":%s}" . -}}
                {{- $wrapped -}}
              type: golang_template_text_v0.3.0
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1budgets/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/budgets/methods/get'
          - $ref: '#/components/x-stackQL-resources/budgets/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/budgets/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/budgets/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/budgets/methods/delete'
        replace: []
    alert_rules:
      id: oci.budgets.alert_rules
      name: alert_rules
      title: Alert Rules
      methods:
        list:
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}~1alertRules/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.alert_rules
            overrideMediaType: application/json
            schema_override:
              $ref: '#/components/schemas/ListAlertRulesResponse'
            transform:
              body: |-
                {{- $wrapped := printf "{\"alert_rules\":%s}" . -}}
                {{- $wrapped -}}
              type: golang_template_text_v0.3.0
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}~1alertRules/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}~1alertRules~1{alertRuleId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}~1alertRules~1{alertRuleId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1budgets~1{budgetId}~1alertRules~1{alertRuleId}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/alert_rules/methods/get'
          - $ref: '#/components/x-stackQL-resources/alert_rules/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/alert_rules/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/alert_rules/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/alert_rules/methods/delete'
        replace: []
    cost_alert_subscriptions:
      id: oci.budgets.cost_alert_subscriptions
      name: cost_alert_subscriptions
      title: Cost Alert Subscriptions
      methods:
        list:
          operation:
            $ref: '#/paths/~1costAlertSubscriptions/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.items
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1costAlertSubscriptions/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1costAlertSubscriptions~1{costAlertSubscriptionId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1costAlertSubscriptions~1{costAlertSubscriptionId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1costAlertSubscriptions~1{costAlertSubscriptionId}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/cost_alert_subscriptions/methods/get'
          - $ref: '#/components/x-stackQL-resources/cost_alert_subscriptions/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/cost_alert_subscriptions/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/cost_alert_subscriptions/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/cost_alert_subscriptions/methods/delete'
        replace: []
    cost_anomaly_event_analytics:
      id: oci.budgets.cost_anomaly_event_analytics
      name: cost_anomaly_event_analytics
      title: Cost Anomaly Event Analytics
      methods:
        summarize_cost_anomaly_event_analytics:
          operation:
            $ref: '#/paths/~1costAnomalyEventAnalytics/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
      sqlVerbs:
        select: []
        insert: []
        update: []
        delete: []
        replace: []
    cost_anomaly_events:
      id: oci.budgets.cost_anomaly_events
      name: cost_anomaly_events
      title: Cost Anomaly Events
      methods:
        list:
          operation:
            $ref: '#/paths/~1costAnomalyEvents/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.items
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1costAnomalyEvents~1{costAnomalyEventId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1costAnomalyEvents~1{costAnomalyEventId}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_events/methods/get'
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_events/methods/list'
        insert: []
        update:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_events/methods/update'
        delete: []
        replace: []
    cost_anomaly_monitors:
      id: oci.budgets.cost_anomaly_monitors
      name: cost_anomaly_monitors
      title: Cost Anomaly Monitors
      methods:
        list:
          operation:
            $ref: '#/paths/~1costAnomalyMonitors/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
            objectKey: $.items
          config:
            queryParamPushdown:
              top:
                paramName: limit
                maxValue: 1000
          request:
            nativeCasing: camel
        create:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1costAnomalyMonitors/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        delete:
          operation:
            $ref: '#/paths/~1costAnomalyMonitors~1{costAnomalyMonitorId}/delete'
          response:
            mediaType: application/json
            openAPIDocKey: '204'
          request:
            nativeCasing: camel
        get:
          operation:
            $ref: '#/paths/~1costAnomalyMonitors~1{costAnomalyMonitorId}/get'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        update:
          config:
            requestBodyTranslate:
              algorithm: naive
          operation:
            $ref: '#/paths/~1costAnomalyMonitors~1{costAnomalyMonitorId}/put'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        disable_cost_anomaly_monitor:
          operation:
            $ref: '#/paths/~1costAnomalyMonitors~1{costAnomalyMonitorId}~1actions~1disable/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
        enable_cost_anomaly_monitor:
          operation:
            $ref: '#/paths/~1costAnomalyMonitors~1{costAnomalyMonitorId}~1actions~1enable/post'
          response:
            mediaType: application/json
            openAPIDocKey: '200'
          request:
            nativeCasing: camel
      sqlVerbs:
        select:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_monitors/methods/get'
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_monitors/methods/list'
        insert:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_monitors/methods/create'
        update:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_monitors/methods/update'
        delete:
          - $ref: '#/components/x-stackQL-resources/cost_anomaly_monitors/methods/delete'
        replace: []
servers:
  - url: https://usage.{region}.oci.oraclecloud.com/20190111
    variables:
      region:
        default: us-ashburn-1
        description: OCI region identifier (e.g. us-ashburn-1, ap-sydney-1); resolves from OCI_CLI_REGION when not supplied in the query.
        x-stackQL-envVar: OCI_CLI_REGION
x-stackQL-config:
  pagination:
    requestToken:
      key: page
      location: query
    responseToken:
      key: opc-next-page
      location: header
